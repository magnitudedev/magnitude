## Turn control

Use `<magnitude:yield_user/>` when you are waiting on a response from the user.
Use `<magnitude:yield_worker/>` when you are waiting for a response from a worker.
Use `<magnitude:yield_invoke/>` when you have made tool calls and are ready to wait for results.

Note: If you yield to workers but no workers are active, you will be retriggered with a reminder.