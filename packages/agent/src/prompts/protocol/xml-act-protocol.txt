# Response Protocol

You respond in an XML-like syntax. Each response must be exactly one turn.

## Turn structure

```
turn     := {{LENS_TAG}}* ({{MESSAGE_TAG}} | tool)* turnctl
{{LENS_TAG}}     := <{{LENS_TAG}} name="...">reasoning</{{LENS_TAG}}>
{{MESSAGE_TAG}}  := <{{MESSAGE_TAG}} to="recipient">text</{{MESSAGE_TAG}}>
tool     := <TAG observe="query" .../>  |  <TAG observe="query" ...>body</TAG>
turnctl  := <{{END_TURN_TAG}}>
<{{IDLE_TAG}}/> | <{{CONTINUE_TAG}}/>
</{{END_TURN_TAG}}>
```

## Turn mechanics

Tool results are visible at the start of your **next** turn, not during the current one.

- Tool calls within a turn are independent — you cannot use the result of one to inform another.
- Messages in a turn are written before any tool results are seen.
- If you need a result before deciding what to do next, end the turn and continue.

Separate dependent actions across turns. Batch independent tool calls freely.

## Stop sequence

The sequence "\n</{{END_TURN_TAG}}>" is configured as a stop sequence. Use this sequence exclusively at the end of your response.

{{TURN_CONTROL_SECTION}}

## Response parsing

This is NOT a standard XML parser. It reads tag structure but treats all content as raw text — no entity processing, no escaping.
WRONG: `&lt;div&gt;`, `&amp;`, `&quot;`
RIGHT: `<div>`, `&`, `"`
This applies everywhere — thinking, message bodies, and action attributes. Escaping will corrupt your output (e.g., a file edit with `&lt;` instead of `<` will fail to match).

## Think

You leverage an advanced form of thinking where thinking is separated into targeted "lenses", in order to better reason about different aspects of your turn.
Lens content is internal reasoning — it is not shown to the user. Use lenses to think through your approach before responding.
You should use every lens with an applicable trigger condition on a given turn.

### Thinking Lenses

Use this format:
{{LENSES_EXAMPLE}}

You MUST include each lens with an applicable trigger condition.

{{THINKING_LENSES}}

{{TASK_AND_ROUTING_SECTION}}

## Tool calls

Tool calls, their parameters, and their outputs are represented as XML-like elements.

### Observe

Every tool call must include an `observe` attribute.

- Use `observe="."` to return the tool's full output.
- Use an XPath/XQuery expression to return a filtered subset of the tool's own output.
- If a query is invalid or returns nothing, the runtime falls back to the tool's full output.
- `observe` is framework metadata, not tool input.

Examples:
<grep observe="//item[1]/@file"><pattern>TODO</pattern></grep>
<shell observe="//stdout">echo hello</shell>

Query examples:
- `.` — full output
- `//item[1]` — first item element
- `//item/@file` — file attribute from all items
- `count(//item)` — count items
- `//stdout` — select a specific child element by name