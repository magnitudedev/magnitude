# Testing lab coverage

Updated 2026-09-21 (UTC). A catalog entry is a planned target, not a qualification result.
Full qualification requires real package installation, UI interaction, generation through the
requested backend, all selected harnesses, CLI, recovery, update, uninstall and cleanup.

| Machines and coverage | Implementation and observed result | Remaining work |
|---|---|---|
| Azure Ubuntu 24.04, x64 Intel, CPU | Complete unpublished-source flow exercised locally: 65/66 passed. Same full flow in GitHub Actions: 64/66 passed. All nine suites ran; both runs cleaned up successfully. | Hermes file-edit correctness fails. GitHub Pi terminal readiness incorrectly required uppercase output; the correction passed in subsequent Fedora qualification. Neither report is green. |
| Azure Fedora 44, x64 Intel / AMD, CPU | All 66 checks passed on both machines, including all nine suites and Pi/OpenCode/Hermes. Workers removed; evidence verified. | This qualifies the existing RPM graph; compilation was performed by the canonical Ubuntu producer. |
| Azure Debian 13, x64 Intel / AMD, and Ubuntu 24.04 x64 AMD, CPU | Debian Intel 65/66 passed; Debian AMD and Ubuntu AMD 64/66 passed. No blocked checks or cleanup errors. | Remaining failures concern Hermes tool editing and terminal generation/interruption. Reports are not green. |
| Azure Red Hat 10, x64 Intel / AMD, CPU | Intel passed all 66 checks. AMD passed 64/66 with no blocked checks. Both completed all nine suites and cleanup. The 1 GiB home-volume failure was fixed and natively verified. | AMD Hermes H5 reported unresolved tools; H7 did not render streamed output. All 296 evidence objects (185,693,726 bytes) were retrieved and verified after cleanup; Azure inventory contains no resources for this run. |
| Azure Ubuntu / Debian / Fedora / Red Hat, arm64 CPU | The corrected Clang producer built and packaged successfully. Ubuntu and Debian each passed 64/66; Fedora and Red Hat each passed 65/66. All nine suites completed, with zero blocked cases or cleanup errors. All 529 evidence objects (435,699,434 bytes) were verified after removal; Azure inventory is empty for this run. | Three harness failures remain. Ubuntu/Debian P4 rejected two aliases of the same native loader; the corrected trusted resolver now passes the complete focused package selection on both guests (8/8 each), using the original packages. All 13 evidence objects (5,119,817 bytes) were verified after cleanup and the run has no remaining Azure resources. Red Hat A7 exposed disappearing configuration-error guidance; the UI fix plus duplicate-prose locator correction now pass the packaged Red Hat ARM app selection (12/12), including A7, with verified evidence and zero resources remaining. |
| Azure Windows Server 2022 / 2025, x64 Intel / AMD, CPU | Four Server recipes, exact native OS identity, license-included allocation, scoped update signing/TLS and real updater verification are implemented. Run `run-42e0e4b9-9597-4c3e-8531-dad5eb9fe3ba` completed on all four targets: 44-45 passed per target, with real package installation, app generation, CLI and most harness checks. | The run exposed four shared lab defects: the shell PID was mistaken for Electron, the Node terminal bridge did not consume Windows named-pipe stdin, C++/CLI dependency values serialized inconsistently, and the updater state directory was pre-created with an ACL the product correctly rejects. All four now have focused local fixes; qualification run `run-2efeb25c-14e0-4223-88cc-23e77cd37bf0` is active on Server 2022. Windows offline isolation remains a separate lab capability to implement. Hermes tool behavior remains visible as a product/model result. Windows 10/11 coverage is deferred by agreement. |
| Azure Linux / Windows, x64 NVIDIA A10 / RTX PRO 6000, CUDA | Backend/device attestation and generation cases implemented. Exact Ubuntu A10 and RTX PRO 6000 recipes are configured in the deployed coordinator. Correct-subscription GPU quota requests require support; case 2609200010000010. | Pinned driver preparation and post-install device/version checks are implemented for Ubuntu 24.04 and Windows Server 2022/2025. Microsoft’s current driver matrix lists both Server versions for A10 vGPU 18.8 and RTX PRO 6000 vGPU 20.2; the pinned Windows downloads match those branches. Actual GPU installation and generation remain unverified pending capacity. The current Azure driver documentation does not establish support for Debian 13, Fedora 44, Red Hat 10 or Windows 10; those combinations are rejected before allocation. CUDA SDK preparation is implemented for Linux x64/ARM64 and Windows x64; Linux x64 natively compiled PTX and linked a shared library against cuBLAS on an Azure CPU VM. ARM64 also compiled PTX and linked cuBLAS natively on an Azure CPU VM; its pinned receipt was retained and all resources removed. Windows x64 also emitted PTX and linked a cuBLAS DLL using the release compiler environment; preparation and a single reboot reconciled successfully, and its diagnostic resources were removed. Actual Linux x64 CUDA source compilation and final packaging passed. GPU execution is not qualified. CPU fallback cannot pass CUDA. |
| Namespace macOS 15 / 26, arm64 CPU / Metal | Both original packaged flows reached terminal results: 11 passed, 5 failed, 50 blocked each. Compilation, DMG packaging, installation and package inspection pass. Direct follow-up proves the stock Apple Paravirtual device executes the required SIMD reduction and matrix multiply despite reporting Apple family flags as false. The exact Developer ID signed inference binary reaches ready state with the pinned Cua shim limited to Apple family 7 and the existing 32 KiB threadgroup-memory limit. Evidence bundle SHA-256: `bcef6ec2b974c6a84d2276b420c1c6a0d72861db8d2daa4b0b6206a6e877d44c`. | A Namespace-only, process-scoped profile now requires stock and profiled executable probes before candidate launch; physical Macs receive no override. Packaged Metal qualification run `run-3717c574-18c4-46c9-a3ff-f7e9739714fa` is active on macOS 15 and 26. Production signing/update trust remains unqualified. |
| Office DGX Spark, Ubuntu 24.04 container, arm64 CUDA | Native adapter allocation, transfer, sandbox namespaces and cancellation cleanup passed. Focused packaged flow passed 13/14 checks: actual DEB installation, app startup/setup, catalog/model download, nonstreamed and streamed generation, invalid requests, and exact CUDA device/runtime-module attestation. One small LFM model was used. All 10 evidence objects / 30,919,709 bytes verified; cleanup errors empty and no lab container remains. | E4 failed because the follow-up did not consume the supplied tool result; its assertion remains failing. This is not a green full-suite qualification: CLI, agent harnesses, update and broader recovery were not selected on Spark. Cloud coordinator Tailscale enrollment still awaits approval. No compilation or benchmarks ran on Spark. Container tests do not establish host DGX OS or physical-desktop coverage. |

Excluded from this implementation: macOS Intel, Amazon Linux, Vulkan, new SUSE/Arch/Omarchy/Alpine
packaging and performance benchmarks. `iterate` warm reuse and historical-release migration are
unfinished and now rejected by CLI/planning admission before upload/allocation; use clean `verify`
mode. These guards are deployed in coordinator v19. Private same-source update testing is not production release
trust or historical migration qualification.

## Verification records

- Current local regression: 644 passed, 14 skipped; targeted type check passes with three existing Effect JSON advisories. Infrastructure Bicep and shell syntax checks pass. These are harness checks, not native app qualification.

- Local unpublished-source complete run: `run-57775e74-63a7-44f2-a95f-85b0c444c626`, 65/66 passed; 180 objects / 109,529,496 bytes verified after cleanup.
- Same GitHub API flow: [GitHub run 35505215545](https://github.com/magnitudedev/magnitude/actions/runs/35505215545), lab `run-7218eda4-d223-4c72-ae35-c815141b6ec7`, 64/66 passed; GitHub artifact includes JSON, JUnit and all 180 verified objects / 121,483,697 bytes. The Pi terminal case was subsequently corrected and passed on Fedora; Hermes failures remain genuine.
- Linux x64 matrix: `run-45350833-4305-4a1a-91fe-d918aed37992`, all 625 objects / 505,640,921 bytes verified. Follow-up Red Hat Intel passes all 66 checks; AMD Hermes failures remain. All 296 Red Hat objects / 185,693,726 bytes verified and workers removed.
- Linux ARM full flows: all 529 objects / 435,699,434 bytes verified after cleanup. Follow-up Ubuntu/Debian package run `run-f4cdd4e6-7649-4fd0-a93e-fc647374b5c8` passes 16/16, 13 objects / 5,119,817 bytes verified. Red Hat ARM app run `run-31db7737-bc92-4567-88cb-b0c5c5884532` passes 12/12, 16 objects / 37,051,749 bytes verified. Ownership inventories are empty.
- macOS 15: `run-b0300452-6167-4608-af0d-42009470b075`, 11 passed / 5 failed / 50 blocked; all 25 objects / 11,414,852 bytes verified. macOS 26 consumes the same graph in `run-a40199ce-c17e-42d2-930a-7ee67514c0dd`, with the same counts; all 18 objects / 5,612,140 bytes verified. Both have no cleanup errors and Namespace inventory is empty. Logs show `hardware calibration did not cover every enabled backend` after detecting the Apple Paravirtual Metal device.
- Mac source delivery uses one digest-verified bundle; a native 3,000-object transfer proof and both actual package flows exercise it. Finder setup applies only to the macOS 15 producer. macOS 26 consumers do not require builder-only Finder permission. No privacy database was modified.
- Windows Server dirty-source run `run-e7b61f92-9f37-4711-a4a0-91a616bea5e4`: compilation passed; packaging blocked on missing OpenSSL before either consumer ran. All five evidence objects / 1,354,681 bytes were retrieved and hash-verified. Cleanup reported no errors and the producer was removed.
- Native Windows dependency proofs cover x64/x86 OS resolution, owned DLL traversal, product-owned runtime PATH, missing imports and wrong architecture. Linux x64/ARM64 and Windows x64 CUDA compiler proofs emit PTX and link cuBLAS. These diagnostics do not establish packaged app or GPU generation qualification. Temporary diagnostic VMs and private probe blobs were removed, with ownership inventories empty.
- NVIDIA A10 and RTX PRO 6000 Linux installers were downloaded, length/hash verified and extracted on native Linux. Each contains the expected unique x64 `libcuda` payload. Preparation now binds installed driver bytes to this pinned payload in a root-owned receipt, allowing dependency verification without weakening ownership requirements for other libraries. Actual GPU driver installation remains unverified.

Retrieve reports and evidence using [USAGE.md](USAGE.md). Evidence is scoped to the run owner.
The source/API, cancellation, cleanup and evidence paths have real verification; this does not turn
product failures or unavailable platforms into passes. Do not rerun unchanged failures without a
specific diagnostic question or relevant fix.

CUDA source package run `run-1bd4ce72-e5ed-408e-a5c3-1557dd82cf6e`: compilation and final packaging passed; six consumer checks blocked because GPU recipes were not configured in that revision. All eight evidence objects / 2,237,522 bytes verified; cleanup empty and producer ownership inventory empty. The new deployment config includes `Standard_NV36ads_A10_v5` and `Standard_NC72ds_xl_RTXPRO6000BSE_v6` with pinned Ubuntu preparation and NVIDIA drivers. Their quota and SKU restrictions remain external prerequisites; configured recipes do not constitute GPU qualification.

Windows build-only proof (2026-09-20): source `257e3f79b824c2f4250c17269e97b3efcc58c9c61f18275fd460655d2f6af986`, artifact graph `3d69afe9416beb2076a083d87e4141fbc7ec47cead6500650cd4dd6eeaa8f0a0`. Three build-log objects / 15,479 bytes plus the artifact manifest were retrieved and hash-verified. Native OS evidence records Windows Server 2025, product type 3; this is not Windows client qualification. The first diagnostic launcher failed before candidate execution because it used environment variables for protected Windows Run Command arguments; the corrected handoff passed native download/upload verification before the successful build. Both diagnostic workers and private inputs were removed. The evidence ZIP remains in the private Azure artifact container at `diagnostics/windows-package-retry-20260920/evidence.zip`.

Resource status at this checkpoint: focused Windows and Namespace qualification runs are active,
so no global empty inventory is claimed. The manual Namespace Metal probe was archived and
removed. Prior Windows attempts, the ARM64 CUDA producer, Windows TLS and desktop probes, and the
focused Hermes worker were removed with verified ownership inventories. Spark's lab container is
absent. The coordinator, database, storage, registry and network are intentionally persistent.
Refresh worker inventory after the active runs. A10 v5 and RTX PRO 6000 v6 XL quotas were last
checked in the correct subscription on September 21 (UTC); both remained zero.

Hermes investigation: RHEL AMD H5 evidence shows the model patching the JSON tool-result
wrapper instead of the file content, then writing that wrapper into the file. That is a real
incorrect edit; its assertion remains failing. H7 evidence contains generated SUNFLOWER
wrapped as SUNFLOW / ER. Display marker matching now handles renderer line wrapping;
focused regression tests pass. Native follow-up `run-fb15fa3e-7a03-4150-872a-644d0391c1c9` reused the same RPM graph on Red Hat AMD: 17/18 checks passed, including H7 TUI generation/interruption and all other Hermes checks except H5. The model supplied literal quote characters to `patch`, producing `"after"` instead of the required `after` followed by a newline. Exact file-effect assertions remain intact. All 34 evidence objects / 40,927,175 bytes verified after cleanup; the worker resource inventory is empty. Interruption lifecycle,
model selection, persisted recovery text and tool-effect assertions remain intact.
