# origin: H-12
# invoke: iq4g32[512]=rand(5)
# invoke: iq4g32[512]=bits(5)
fn probe[K](w: &tensor[K] iq4g32) -> f32 where K % 256 == 0:
    let mut total = 0.0
    for k in 0..K:
        total = total + f32(w[k])
    return total
