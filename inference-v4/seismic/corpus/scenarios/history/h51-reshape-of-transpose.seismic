# origin: H-51
# invoke: f32[2,3]=seq
fn probe[M, N](x: &tensor[M, N] f32) -> tensor[N, M] f32:
    let t = x.T
    let r = reshape(t, (M, N))
    let back = reshape(r, (N, M))
    return to_owned(back)
