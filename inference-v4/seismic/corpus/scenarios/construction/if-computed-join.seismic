# origin: C11:if_computed_join
# cell: value-computed-tensor/join
# invoke: f32[4]=1,2,3,4 ; f32[4]=5,6,7,8 ; f32:1
fn probe(a: &tensor[4] f32, b: &tensor[4] f32, x: f32) -> f32:
    let mut t = a + b
    if x > 0.0:
        t = a - b
    return reduce(t, 0, sum)
