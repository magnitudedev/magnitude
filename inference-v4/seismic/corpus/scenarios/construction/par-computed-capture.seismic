# origin: C11:par_computed_capture
# invoke: f32[4]=1,2,3,4 ; f32[4]=0,0,0,0
fn probe[N](x: &tensor[N] f32, dst: &mut tensor[N] f32):
    let y = x * 2.0
    parallel for k in 0..N:
        dst[k] = y[k] + 1.0
