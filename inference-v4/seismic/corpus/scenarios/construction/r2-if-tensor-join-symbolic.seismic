# origin: C11:r2_if_tensor_join_symbolic
# invoke: f32[4]=1,1,1,1 ; f32[4]=2,2,2,2 ; bool:true
fn probe[N](a: &tensor[N] f32, b: &tensor[N] f32, c: bool) -> tensor[N] f32:
    let mut t = to_owned(a)
    if c:
        t = to_owned(b)
    return t
