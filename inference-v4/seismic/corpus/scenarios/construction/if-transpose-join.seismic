# origin: C11:if_transpose_join
# rejected: "cannot cross an owned boundary"
fn probe(x: &tensor[3,3] f32, c: bool) -> tensor[3,3] f32:
    let mut v = to_owned(x)
    if c:
        v = x.T
    return v
