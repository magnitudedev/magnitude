# origin: C14:s14f
# invoke: f32[5]=rand(50) ; bool:true
# invoke: f32[5]=rand(50) ; bool:false
fn probe[N](x: &tensor[N] f32, c: bool) -> f32:
    let mut t = x + x
    if c:
        let u = x * x
        t = u
    return t[0]
