# origin: C17:atomic_vs_read_same_cell
# rejected: "independent visits"
fn probe[N](x: &tensor[N] i32, out: &mut tensor[1] i32, seen: &mut tensor[N] i32):
    parallel for i in 0..N:
        atomic(add, out[0], x[i])
        seen[i] = out[0]
