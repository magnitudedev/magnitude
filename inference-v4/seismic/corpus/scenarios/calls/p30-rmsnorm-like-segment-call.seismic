# origin: C13:p30
# invoke: i32[2,4]=seq ; i32[2,4]=zero
fn total[N](t: tensor[N] i32) -> i32:
    return reduce(t, 0, sum)

fn probe(x: &tensor[2, 4] i32, y: &mut tensor[2, 4] i32):
    parallel for r in 0..2:
        let t = x[r]
        let s = total(t * t)
        y[r] = t + s
