# origin: C13:p27e
# invoke: i32[6]=seq
fn seed[N](x: &tensor[N] i32) -> i32:
    return reduce(x, 0, sum) + i32(N) * 100

fn probe(input: &tensor[6] i32) -> i32:
    let mut r = tensor[4] i32
    r[:] = ones_like(r)
    return seed(r)
