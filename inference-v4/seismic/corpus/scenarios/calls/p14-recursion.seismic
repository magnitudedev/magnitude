# origin: C13:p14
# rejected: "recursi"
fn a[N](x: &tensor[N] i32) -> i32:
    return b(x)

fn b[N](x: &tensor[N] i32) -> i32:
    return a(x)

fn probe(input: &tensor[4] i32) -> i32:
    return a(input)
