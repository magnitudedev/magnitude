# origin: C13:p03f
# invoke: i32[8]=seq ; i32[2]=1,4
fn seed[N](x: &tensor[N] i32) -> i32:
    return i32(N)

fn probe(input: &tensor[8] i32, bounds: &tensor[2] i32) -> i32:
    let lo = bounds[0]
    let hi = bounds[1]
    let mut r = 0
    if hi > lo:
        r = seed(input[lo:hi])
    return r
