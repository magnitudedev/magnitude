# origin: C13:p09
# cell: call-portable/parallel
# invoke: i32[8]=seq ; i32[4]=zero
fn seed[N](x: &tensor[N] i32) -> i32:
    return reduce(x, 0, sum) + i32(N) * 100

fn probe(input: &tensor[8] i32, out: &mut tensor[4] i32):
    parallel for i in 0..4:
        out[i] = seed(input[0:i + 1])
