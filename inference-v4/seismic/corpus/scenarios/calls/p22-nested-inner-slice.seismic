# origin: C13:p22
# rejected: "lower bound"
fn inner[M](x: &tensor[M] i32) -> i32:
    return i32(M)

fn outer[N](x: &tensor[N] i32) -> i32:
    return inner(x[1:N])

fn probe(input: &tensor[4] i32) -> i32:
    return outer(input)
