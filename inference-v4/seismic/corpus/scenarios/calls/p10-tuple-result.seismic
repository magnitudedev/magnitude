# origin: C13:p10
# cell: value-tuple/result
# invoke: i32[4]=seq
fn split[N](x: &tensor[N] i32) -> (i32, tensor[N] i32):
    return reduce(x, 0, sum), x * 2

fn probe(input: &tensor[4] i32) -> (i32, tensor[4] i32):
    let (s, t) = split(input)
    return s + 1, t + 1
