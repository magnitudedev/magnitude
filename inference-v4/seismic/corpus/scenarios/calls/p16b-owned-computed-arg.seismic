# origin: C13:p16b
# cell: value-computed-tensor/arg
# invoke: i32[4]=seq
fn own[N](x: tensor[N] i32) -> i32:
    let mut y = x
    y[0] = 100
    return reduce(y, 0, sum)

fn probe(input: &tensor[4] i32) -> i32:
    return own(input * 2)
