# origin: C13:p13a
# cell: call-target-helper/sl
# rejected: "written as"
fn cpu_sum[N](x: &tensor[N] i32) -> i32 for cpu:
    return reduce(x, 0, sum)

fn probe(input: &tensor[4] i32) -> i32:
    return cpu_sum(input)
