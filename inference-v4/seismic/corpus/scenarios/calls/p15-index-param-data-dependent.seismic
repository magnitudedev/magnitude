# origin: C13:p15
# rejected: "expects index"
fn pick[N](x: &tensor[N] i32, i: index[N]) -> i32:
    return x[i]

fn probe(input: &tensor[4] i32, which: &tensor[1] i32) -> i32:
    let k = which[0]
    return pick(input, k)
