[package]
name = "seismic-native-example"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
seismic = { path = "../../../api" }

[build-dependencies]
seismic-build = { path = "../../../build" }

