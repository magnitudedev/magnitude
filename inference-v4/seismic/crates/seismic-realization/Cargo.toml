[package]
name = "seismic-realization"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/realization.rs"

[dependencies]
seismic-lang.workspace = true
magnitude-solver = { path = "../../../solver" }
