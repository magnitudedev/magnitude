[package]
name = "vulkan-facts"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone probe (not a workspace member): device facts and driver behaviour for the Vulkan
# backend spec §16. See ../README.md.
[workspace]

[dependencies]
ash = "0.38"
glslang = "0.8.1"
serde_json = "1"
