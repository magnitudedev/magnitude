"""Host machine discovery, measurement ownership, and result publication.

Nothing here participates in kernel selection. The endpoint facts a kernel may
consult are reported as a ``Capability`` by the driver, not discovered here.
"""
