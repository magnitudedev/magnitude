name: macOS CPU inference acceptance

on:
  workflow_call:
    inputs:
      artifact_run_id:
        required: true
        type: string
      model:
        type: string
        default: 'lfm2.5-2.6b:gguf:q4'

permissions:
  contents: read
  actions: read

jobs:
  intel-cpu:
    runs-on: macos-15-large
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/download-artifact@v4
        with:
          name: host-darwin-x64
          path: candidate
          run-id: ${{ inputs.artifact_run_id }}
          github-token: ${{ github.token }}
      - name: Run actual CPU inference from the final archive
        env:
          CATALOG_MODEL: ${{ inputs.model }}
        run: python packages/release/scripts/test-unix-cpu.py --artifact-directory candidate --output-directory evidence --model "$CATALOG_MODEL"
      - name: Retain inference evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: intel-cpu-inference-${{ inputs.artifact_run_id }}
          path: |
            evidence/results.json
            evidence/server.log
            evidence/cleanup.json
          if-no-files-found: warn
