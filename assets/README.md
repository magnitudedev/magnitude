# Shared assets

This directory owns source assets shared across Magnitude products and build targets.

- `brand/` contains canonical product marks and identity artwork.
- Client builds import these source files and bundle them into their own output.
- Documentation-specific artwork remains under `docs/` and does not serve as a runtime dependency.
