export function localProviderSavedEndpointToast(providerDisplayName: string): string {
  return `Saved endpoint for ${providerDisplayName}`
}

export function localProviderAddedModelToast(modelId: string): string {
  return `Added model ${modelId}`
}

export function localProviderSavedApiKeyToast(providerDisplayName: string): string {
  return `Saved API key for ${providerDisplayName}`
}

export function localProviderSavedEmptyApiKeyToast(providerDisplayName: string): string {
  return `Saved empty API key for ${providerDisplayName}`
}
